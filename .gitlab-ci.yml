image: python:latest

stages:
  - test
  - deploy

before_script:
  - python -V
  - pip install jinja2 -U

pages:
  stage: deploy
  script:
  - python build.py
  artifacts:
    paths:
    - public
  only:
  - master
